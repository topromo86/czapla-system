# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# Szablon bez sekretów - dokumentuje, co trzeba ustawić.
!.env.example

# vercel
.vercel

# claude code local settings (machine-specific, not shared)
.claude/settings.local.json

# typescript
*.tsbuildinfo
next-env.d.ts

/app/generated/prisma

.vercel
.env*
serwer-unixstorm/dane/*
!serwer-unixstorm/dane/.htaccess

# Lista haseł kadry generowana przez prisma/hasla-trenerow.ts.
# Powstaje obok projektu, rozdaje się ją osobiście i kasuje - nigdy do repo.
hasla-instruktorow-*.txt
